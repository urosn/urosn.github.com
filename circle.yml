machine:
  environment:
    USER_NAME: urosn
    USER_EMAIL: urosn@beotel.rs
  ruby:
    version: 2.3.3

dependencies:
  pre:

test:
  post:
    - bundle exec jekyll build
    - bundle exec htmlproof ./_site --only-4xx --href-ignore "#"

deployment:
  production:
    branch: master
    commands:
      - ./bin/automated
