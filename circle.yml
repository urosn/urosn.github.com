machine:
  environment:
    USER_NAME: urosn
    USER_EMAIL: urosn@beotel.rs
  ruby:
    version: 2.2.4

dependencies:
  pre:
    - ./bin/setup

test:
  post:
    - bundle exec jekyll build
    - bundle exec htmlproof ./_site --only-4xx --href-ignore "#"

deployment:
  production:
    branch: master
    commands:
      - ./bin/automated
